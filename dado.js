let dado = {
    lado: 6,
    nome: "lol",

    jogar: function() {
        return p = Math.ceil(Math.random() *(6-1)+1);

    },
};
console.log(dado.jogar())

let contarJogadas = {

    contador: function() {
        return p+1;
    },
};
console.log(contarJogadas.contador())

let boletim = {
    nome: "David",
    idade: 19,
    
}
